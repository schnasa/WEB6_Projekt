import { LvaFormErrorMessages } from './lva-form-error-messages';

describe('LvaFormErrorMessages', () => {
  it('should create an instance', () => {
    expect(new LvaFormErrorMessages()).toBeTruthy();
  });
});
