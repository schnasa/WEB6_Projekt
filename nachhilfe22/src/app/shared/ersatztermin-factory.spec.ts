import { ErsatzterminFactory } from './ersatztermin-factory';

describe('ErsatzterminFactory', () => {
  it('should create an instance', () => {
    expect(new ErsatzterminFactory()).toBeTruthy();
  });
});
